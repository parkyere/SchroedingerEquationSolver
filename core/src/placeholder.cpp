// Intentionally empty build placeholder.
//
// The core library is populated *test-first* through the TDD loop
// (see docs/TDD_RULES.md). This file exists only so that the static
// library `sesolver_core` is linkable while it has no real sources yet.
//
// DELETE this file as soon as the first real translation unit is added
// under core/src/ and listed in core/CMakeLists.txt.
